sudo: required

services:
 - docker

jobs:
 include:
  - stage: build docker image
    script:
     - docker build -t klibio/io.klib.docker.java --file ./Dockerfile-alpine.txt . 
     - docker images ls